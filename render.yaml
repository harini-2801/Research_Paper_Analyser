services:
  - type: web
    name: research-paper-analyzer-api
    env: python
    buildCommand: "pip install -e ."
    startCommand: "uvicorn rpra.server:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
      - key: OPENAI_API_KEY
        sync: false
